// @flow
// Default height for the toolbar area to ensure visual consistency.
export const STANDARD_TOOLBAR_HEIGHT = 56;

// Default values for pagination if not provided by the user.
export const DEFAULT_PAGE_SIZE_OPTIONS: Array<number> = [10, 25, 50, 100];
export const DEFAULT_INITIAL_PAGE_SIZE: number = 10;
