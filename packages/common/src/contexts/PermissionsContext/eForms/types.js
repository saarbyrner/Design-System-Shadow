// @flow
export type EFormsPermissions = {
  canSubmitForms: boolean,
  canViewForms: boolean,
  canEditForms: boolean,
  canDeleteForms: boolean,
  canManageFormTemplates: boolean,
  canViewTryout: boolean,
};
