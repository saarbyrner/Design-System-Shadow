// @flow

// This is for distinguishing 'special' Organisation.association_name values that require different logic or UI0
// Use to avoid additional feature flag or relying on window.organisationSport === 'nfl' for example
// We would NOT add all associations here

export default {
  nfl: 'National Football League',
};
