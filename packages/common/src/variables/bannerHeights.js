// @flow
const bannerHeights = {
  desktop: '60px',
  tablet: '50px',
};

// For usage in calculations,
// e.g. height: calc(100vh - ${bannerHeightsValue.desktop + otherElement}px)
export const bannerHeightsValue = {
  desktop: 60,
  tablet: 50,
};

export default bannerHeights;
