// @flow
import { Grid } from '@kitman/playbook/components';

const PlayerView = () => {
  return (
    <Grid container data-testid="PlayerView">
      TODO: Player View
    </Grid>
  );
};

export default PlayerView;
