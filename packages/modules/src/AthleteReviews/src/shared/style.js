// @flow
import { colors } from '@kitman/common/src/variables';

export default {
  // TODO: being evaluated with design if this should be made
  // default link behaviour
  linkStyle: {
    '&:visited': { color: colors.grey_200 },
    '&:hover': { color: colors.grey_200 },
  },
  formField: {
    width: 250,
  },
};
