// @flow
import { css } from '@emotion/react';

const style = {
  stackedActivity: css({
    width: '50%',
  }),
  stackedActivities: css({
    display: 'flex',
    flexWrap: 'wrap',
  }),
};

export default style;
