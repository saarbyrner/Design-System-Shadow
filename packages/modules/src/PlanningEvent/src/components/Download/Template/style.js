// @flow
import { css } from '@emotion/react';

const style = {
  template: css({
    padding: '10px',
    width: '790px',
  }),
  addAthletesMessage: css({
    textAlign: 'center',
    paddingTop: '50%',
    fontSize: '2rem',
  }),
};

export default style;
