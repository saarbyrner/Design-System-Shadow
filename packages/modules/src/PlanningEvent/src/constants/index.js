// @flow
const DUPLICATED_EVENT_HASH = '#duplicated';
const EVENT_TYPES = {
  game_event: 'Game',
  session_event: 'Session',
  custom_event: 'Custom',
};

const REQUEST_STATUS = {
  LOADING: 'LOADING',
  SUCCESS: 'SUCCESS',
  FAILURE: 'FAILURE',
};

export { DUPLICATED_EVENT_HASH, EVENT_TYPES, REQUEST_STATUS };
