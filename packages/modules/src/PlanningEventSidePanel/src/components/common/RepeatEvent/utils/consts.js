// @flow

import { RRule } from 'rrule';

export const defaultOptionValue: RRule | null = null;

export const customOptionValue = 'Custom';

export const everyWeekdayArray = [
  RRule.MO,
  RRule.TU,
  RRule.WE,
  RRule.TH,
  RRule.FR,
];
