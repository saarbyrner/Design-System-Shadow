// @flow

import { colors } from '@kitman/common/src/variables';

export const APPROVED_STATUS_COLORS = {
  approved: colors.green_200,
  requested: colors.orange_400,
  denied: colors.red_100,
  expired: colors.red_100,
  withdrawn: colors.red_100,
  pending: undefined,
};
