import { connect } from 'react-redux';
import AppComponent from '../components/App';

const mapDispatchToProps = () => ({});

const App = connect(mapDispatchToProps)(AppComponent);

export default App;
